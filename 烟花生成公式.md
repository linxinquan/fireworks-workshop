# 烟花生成公式总结

> 本文档以 `烟火小作坊.html` 中的代码为准，梳理「原料 → 搅拌 → 压实 → 绽放效果」的完整推导链路。
> 文档只做总结，不改动页面行为。
>
> 代码位置：`parseRecipe()` 配方解析、`spawnBurst()` 形态渲染、`spawnRainbowBurst()` 彩虹分支。

---

## 一、总览：四步推导

```
① 原料组合（最多 4 个）      ② 搅拌进度 mix        ③ 压实程度 pack        ④ 屏幕自适应
   roles → power/count        0 ~ 150 分（=0~100%）   light / mid / tight     SCALE 系数
   color → 颜色数组             <40 / 40~115 /          0.8x / 1x / 1.25x       随视口宽收放
   shape → 绽放形态             115~135 / >135
            ↓                      ↓                      ↓                      ↓
                      「今夜绽放」结果卡：名字 = 主色 + 花型
```

---

## 二、原料角色

配方最多选 4 个原料，按 `type` 分三类角色：

| 角色 | `type` | 参与计算 | 作用 |
|------|--------|---------|------|
| 基础药 | `base` | `bases` | 决定是否哑火、粒子数量与爆裂速度的量级 |
| 着色剂 | `color` / `spark` | `colors` / `sparks` | 决定颜色数组；`spark` 额外贡献银白闪光 |
| 花型剂 | `shape` | `shapes` | 决定绽放几何形态 |

### 基础药参数

```
baseCount = max(1, bases.length)                 // 缺省按 1 算
power     = 1 + (Σ ing.power − 0.8)              // 各基础药 power 求和后减去基准 0.8

count = round((70 + 40 × baseCount) × power)     // 粒子数量
speed = 240 + 40 × baseCount                     // 初速 px/s
```

| 基础药 | power |
|--------|-------|
| 硝酸钾 saltpeter | 1 |
| 硫磺 sulfur | 1 |
| 木炭粉 charcoal | 0.8 |

**哑火判定**：`bases.length === 0` → `dud = true`，引线烧完只冒青烟，不升空（`spawnDudSmoke()`），名字为「哑火的小爆竹」。

---

## 三、着色（颜色公式）

### 3.1 基础主色

```
有着色剂 → colorList = 各着色剂色值
无着色剂 → colorList = ["#ffd166", "#fff3b0"]   // 默认金白
```

### 3.2 两条「翻车 / 覆盖」规则（按顺序覆盖）

| 条件 | 结果 | 依据 |
|------|------|------|
| 含钠盐 sodium | `colorList = ["#ffd60a","#ffb703"]` 强制覆盖全部颜色 | 钠的 589nm 亮线过亮，盖住其他色 |
| 含铜盐 copper **且** `mix < 40` | `colorList = ["#9fd8c0","#d8f0e4","#cfd6e4"]`（绿白灰） | 铜盐蓝色温窗极窄，搅拌不足温度偏 → 翻成绿白 |

### 3.3 光泽混合

每个主色按 `GLOW` 表补「同系高光 + 近白」，最后追加 `#ffffff`，让烟花有光泽层次：

```
glossy = [主色1, GLOW[主色1][0], GLOW[主色1][1], 主色2, ...] + ["#ffffff"]
```

### 3.4 主色命名（用于结果卡名字与图鉴）

逐条 `if` 覆盖，**后写胜出**：

```
默认 gold
strontium          → red
barium             → green
copper && mix≥40   → blue
copper && mix<40   → green        // 翻车也改名字
sodium             → gold
style === rainbow  → rainbow
mix > 135          → mystery
```

映射表：`COLOR_NAME = {red:"赤", green:"翠", blue:"蓝", gold:"金", purple:"紫", white:"月白", rainbow:"彩"}`

---

## 四、花型（形态公式）

### 4.1 花型判定优先级

```
style = 第一个 shape 原料的 shape
若 shapes.length > 1 且 random < 0.35  → style = shapes[1].shape    // 双花型剂有 35% 取第二个
若 mix > 135                            → style = "mystery"           // 强制覆盖
```

### 4.2 九种形态的渲染实现

| style | 名称 | 触发原料 | 渲染公式要点 |
|-------|------|---------|-------------|
| `peony` | 牡丹 | 默认 | `ang = i/count·2π + 抖动`，`sp = speed·(0.62+rand·0.65)` 圆润均匀 |
| `willow` | 垂柳 | 月光花瓣 | 只向下 `ang = π/2 ± 0.8`，`sp` 小，`gravity 0.34`、长拖尾 |
| `chrys` | 菊花 | 雷鸟羽毛 | 径向 `sp = speed·(0.65+rand·0.9)` + 长拖尾 + 闪烁 |
| `ring` | 光环 | 星尘 | 速度带极窄 `sp = speed·(0.94+rand·0.1)` → 同心圆环 |
| `star` | 星绽 | 凤羽灰 | 五角星轮廓 `r = 0.45 + 0.55·max(0,cos(5θ))²` |
| `double` | 双子 | 龙鳞粉 | 先放一发 peony，`0.35~0.7s` 后 80% 概率再炸一发（随机 peony/chrys/star/ring） |
| `comet` | 彗星 | 妖精之泪 | 扇形向上 `ang = π/2 ± 1.2`，超长拖尾 14 |
| `rainbow` | 虹 | 独角兽鬃毛 | 走独立分支 `spawnRainbowBurst()`，粒子 hue 随时间旋转 `hueSpeed 0.8` |
| `mystery` | 谜团 | 影猫皮毛 / mix>135 | 随机两种形态先后各炸一发 |

### 4.3 花型附加参数

```
willow  → trail ≥ 12, gravity 0.34, count × 1.1
chrys   → trail ≥ 11, gravity 0.05, flicker
ring    → count × 0.7, speed × 0.85
comet   → trail ≥ 14, gravity 0.16, count × 0.9
double  → big = true, count × 0.8
star    → count × 0.85, flicker
mystery → big = true, trail ≥ 7
```

### 4.4 特效合并（遍历所有原料）

| effect | 效果 |
|--------|------|
| `sparkle` | `sparkle = true`（铝粉银闪） |
| `trail` | `trail ≥ 9`，`gravity 0.3`，`styleWillow`（月光花瓣） |
| `trail2` | `trail ≥ 13`，`gravity 0.16`（妖精之泪） |
| `thunder` | `big = true`，`flicker`，`trail ≥ 7`（雷鸟羽毛） |
| `glitter` | `flicker + sparkle`（星尘） |
| `crackle` | `flicker`（凤羽灰） |

---

## 五、搅拌进度（mix，0 ~ 150 分 = 0 ~ 100%）

> 内部 `state.mix` 是「拖动行程折算的累计分」，量程 0~150；**界面统一用百分比展示**：`进度 = min(100, round(mix/150×100))`。
> 一个完整来回（左→右→左）折算约 +40 分，即进度约 +27%（`MIX_PER_UNIT = 10`/单位行程）。

分段乘算（下表同时给出对应的界面进度阈值）：

| 内部区间 | 进度区间 | 含义 | 参数变化 |
|------|------|------|---------|
| `mix < 40` | `< 27%` | 拌不匀 | `count × 0.65`，`speed × 0.8`，`size × 0.8` |
| `40 ≤ mix ≤ 115` | `27% ~ 77%` | 最佳区间 ★ | 无额外乘算，保持基准 |
| `115 < mix ≤ 135` | `77% ~ 90%` | 偏猛 | `count × 1.15`，`speed × 1.1`，`size × 1.15` |
| `mix > 135` | `> 90%` | 过度搅拌 | `count × 1.3`，`speed × 1.25`，`size × 1.3`，`big = true`，**强制 mystery** |

附加耦合：`mix < 40`（进度 < 27%）时铜盐翻成绿白（见 3.2）。

---

## 六、压实程度（pack）

| pack | 名称 | 参数变化 | 隐藏 |
|------|------|---------|------|
| `light` | 轻压 | `speed × 0.82`，`count × 0.8` | — |
| `mid` | 适中 | 无变化（基准） | — |
| `tight` | 紧压 | `speed × 1.15`，`count × 1.25`，`size × 1.12` | 非 double/mystery 时 30% 概率触发 `tightDouble` 二次绽放 |

---

## 七、屏幕自适应

```
params.speed   = round(speed × SCALE)
params.gravity = gravity × SCALE
params.size    = size × max(0.85, SCALE)
```

`SCALE` 随视口宽度收缩，避免同样的速度在大屏是「小花」、在手机小屏糊满全屏。

---

## 八、结果命名

```
name = dud ? "哑火的小爆竹" : (COLOR_NAME[colorName] || "彩") + (SHAPE_NAME[style] || "花")
若 style === "mystery" 且非哑火 → name = "神秘" + SHAPE_NAME[style]
```

`SHAPE_NAME = {peony:"牡丹", willow:"垂柳", chrys:"菊花", ring:"光环", star:"星绽", double:"双子", comet:"彗星", rainbow:"虹", mystery:"谜团"}`

---

## 九、异常 / 待办

1. **`flower` 是稀有孤儿分支**：`spawnBurst()` 中有 `style === "flower"` 的花瓣渲染（`sp = speed·(0.25 + |sin(petals·θ)|·0.9)`），但没有任何原料的 `shape` 为 `flower`。它只会在 `mystery` 随机抽到时出现（`styles` 数组含 `"flower"`），因此概率很低。已在 `SHAPE_NAME` 中补上「花瓣」，并作为图鉴的稀有收集项。

---

## 十、图鉴可收集项汇总

基于以上公式，可被「成功触发」记录的收集团（共 24 项，页面图鉴实现一致）：

| 分组 | 数量 | 触发条件 |
|------|------|---------|
| 花型图鉴 | 10 | 绽放 `style` 命中对应形态（含 `flower` 这一稀有形态） |
| 颜色图鉴 | 6 | 绽放 `colorName` 命中：red / green / blue / gold / rainbow / mystery |
| 隐藏成就 | 8 | 哑火、铜盐翻车、金光盖色、紧压二次绽放、过搅失控、纯火药、满配方、满配方出彩虹 |

> 说明：`colorName` 只会取到 `red / green / blue / gold / rainbow / mystery` 六种值，因此颜色图鉴按这六种建立，`purple` / `white` 在当前公式下不可达。

### 页面实现要点

- 数据定义：`ACHIEVEMENTS` / `ACH_GROUPS`，持久化 key `fw_workshop_achievements_v1`。
- 解锁时机：`launch()` 中 `parseRecipe()` 之后调用 `achUnlock(r)`，即**只有真正点燃并绽放/哑火后**才记录，未燃放不记录。
- 结果卡提示：`showResult()` 渲染 `#rcNew`，只在本次有新解锁时显示「🆕 图鉴新收录」。
- 图鉴弹层：顶部「🏆 烟花图鉴」按钮打开 `#albumDialog`，`renderAlbum()` 渲染分组网格 + 总进度条。
